1. Create virtual environment and activate
2. Run 'pip install -r requirements.txt'
3. Install playwright by following on screen instructions following 'playwright Install'
4. Run with 'fastapi dev' while inside virtual environment
